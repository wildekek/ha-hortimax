__pycache__/
.venv/

# Ridder's API documentation — not ours to publish
postman/

# Local HA test instance (contains the API key in .storage)
ha-config/
.DS_Store

# Personal working notes, not for the repo
todo.md
