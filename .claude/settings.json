{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "printf %s '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"Versioning reminder: before completing this push, bump the version in custom_components/hortimax/manifest.json and create a matching v-prefixed git tag (e.g. v0.1.0); push the tag (git push --follow-tags) and create a GitHub Release for it (gh release create vX.Y.Z). This is a reminder, not a block.\"}}'"
          }
        ]
      }
    ]
  }
}
